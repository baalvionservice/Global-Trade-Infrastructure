/**
 * @file src/app/page.tsx
 * @description Root entry point. 
 * Re-exported from (public) to avoid parallel route collision while maintaining clean URL structure.
 */
export { default } from './(public)/page';
